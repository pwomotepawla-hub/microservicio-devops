name: CI Pipeline

on:
  push:
    branches:
      - develop
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Run build
        run: echo "Build OK"

      - name: Run tests
        run: echo "Tests OK"          echo Add other actions to build,
          echo test, and deploy your project.
